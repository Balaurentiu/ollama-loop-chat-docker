version: '3.8'
services:
  ollama-chat:
    build: .
    ports:
      - "5050:5000"
    restart: unless-stopped
    volumes:
      - ./settings.json:/app/settings.json
      - ./chat.json:/app/chat.json
